# Uncomment this line to define a global platform for your project
platform :ios, '10.0'

source 'https://cdn.cocoapods.org/'
source 'git@github.com:windkisshao/CTPrivateRepo.git'

use_frameworks!
use_modular_headers!

target 'A' do

  pod "CTMediator"

  # private pods
  pod "HandyFrame"
  
  pod "B_Category", :path => "../B_Category"
 
end
